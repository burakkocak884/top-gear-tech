

<div class = "app_whole">


Appointment for <%= link_to "#{@appointment.customer.first_name} #{@appointment.customer.last_name}", customer_path(@appointment.customer.id) %> at <%= @appointment.garage.name %> 
<h3 class = "titles">Date: <h2 style = "color: blue"><%= @appointment.date %><h2></h3>
<h3 class = "titles">Type of Service: </h3> <h2 style = "color :blue"><%= @appointment.description %></h2>

<h3 class = "titles">For : </h3><h2><%= @appointment.vehicle.year %>-<%= @appointment.vehicle.make %>-<%= @appointment.vehicle.model %></h2> 


<%= link_to "Edit this Appointment's info", appointments_edit_path(id: @appointment.id) %></br>
<%= link_to "Delete #{@appointment.date}", appointment_path(@appointment),  :method => :delete %>

<h4><%= link_to "Back to Appointments", garage_appointments_path(@appointment.garage.id) %></h4>

</h2>

