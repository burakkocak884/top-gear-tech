

<h1 style = " padding: 20px"><%= @user.email.split("@")[0].upcase %>'s garages</h1>
<div id ="garages-list">
<ul>
<% @garages.each do |g| %>
	<li><%= link_to g.name, garage_path(g) %></li>
	<%end %>



<li ><%= link_to "Add a New Garage", new_garage_path %></li>
</ul>
<div id ="garages-list"></div>
